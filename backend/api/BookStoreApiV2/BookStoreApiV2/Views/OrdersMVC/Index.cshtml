@model IEnumerable<BookStoreApiV2.Models.Order>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.orDateAndTime)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Coupon.coCode)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.User.uFName)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.orDateAndTime)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Coupon.coCode)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.User.uFName)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.oId }) |
            @Html.ActionLink("Details", "Details", new { id=item.oId }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.oId })
        </td>
    </tr>
}

</table>
